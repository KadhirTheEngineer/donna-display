{
  "name": "donna-display",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"node --watch server/server.mjs\"",
    "build": "tsc --noEmit && vite build",
    "start": "node server/server.mjs",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "display:command": "node scripts/display-command.mjs"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.2.1",
    "concurrently": "^9.2.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.1"
  }
}
